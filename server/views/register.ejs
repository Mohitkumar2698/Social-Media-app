<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register | GameCore</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        min-height: 100vh;
        box-shadow: inset 1000px 1000px 1000px 1000px rgb(0, 0, 0, 0.7);
        background-image: url("<%= bgImage %>");
        background-size: cover;
        background-repeat: no-repeat;
        color: white;
        font-family: "Trebuchet MS", "Lucida Sans Unicode", "Lucida Grande",
          "Lucida Sans", Arial, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
      }
      button {
        color: white;
        background-color: red;
        padding: 0.5rem 1rem;
        font-size: 1.2rem;
        border-radius: 5px;
        border: none;
        cursor: pointer;
      }
      form {
        display: flex;
        align-items: center;
        flex-direction: column;
        gap: 1rem;
        border: 1px solid rgba(255, 0, 0, 0.358);
        width: 30vw;
        padding: 2rem 1rem;
        border-radius: 5px;
        backdrop-filter: blur(30px);
      }
      input {
        background-color: #0000006c;
        color: white;
        border: none;
        padding-left: 1rem;
        font-size: 1.2rem;
        border-radius: 5px;
        width: 100%;
        height: 7vh;
      }
    </style>
  </head>
  <body>
    <form action="/" method="post">
      <input type="text" name="username" placeholder="Enter Username :>" />
      <input type="password" name="password" placeholder="Enter Password :>" />
      <input type="email" name="email" placeholder="Enter Email :>" />
      <input type="number" name="age" placeholder="Enter Age :>" />
      <div
        class="gender"
        style="display: flex; gap: 1rem; align-items: center; max-width: 100%"
      >
        Gender:
        <input type="radio" name="gender" id="gender" value="male" />Male
        <input type="radio" name="gender" id="gender" value="female" />Female
        <input type="radio" name="gender" id="gender" value="other" />Other
      </div>
      <input type="text" name="game" placeholder="Enter Favoraite Game :>" />
      <div class="submit" style="display: flex; gap: 1rem; align-items: center">
        <button type="submit">Register</button>
        <p>Already in? <a href="/login">login</a></p>
      </div>
    </form>
  </body>
</html>
